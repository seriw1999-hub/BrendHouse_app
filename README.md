
Quick start:

1. Unzip and `cd ders6_updated`
2. `npm install`
3. Import `db_init.sql` into MySQL (e.g., `mysql -u root -p < db_init.sql`)
4. Adjust MySQL password in `app.js` if needed.
5. `npm run dev` or `npm start`
6. Open http://localhost:3000
7. 
